// Get DOM elements
var highscoresList = document.getElementById("highscores");
var clearButton = document.getElementById("clear");
var scoreEntry = {
  score: runningScore,
  initials: initialsInput,
};

//create an empty array
var highScores = []

// Push the user score to the high scores array
highScores.push(scoreEntry);

//need to sort high scores from highest to lowest

// change the high scores to strings
highScores.toString

// Add an event listener to the clear button
clearButton.onclick = function () {
 
};

function clearHighscores() {
 
}



// Load highscores from local storage on page load
window.onload = function () {
  // Load highscores from local storage and display them
};

startButton.addEventListener("click", startQuiz);